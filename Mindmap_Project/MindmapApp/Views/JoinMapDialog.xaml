<Window x:Class="MindmapApp.Views.JoinMapDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        Background="Transparent">
    <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="16" Padding="24" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid Width="380">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Tham gia Mindmap" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                <TextBlock Text="Nhập mã share để truy cập sơ đồ." Margin="0,6,0,0" Foreground="#778899"/>
            </StackPanel>

            <Border Grid.Row="1" Background="{DynamicResource BackgroundBrush}" CornerRadius="10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10">
                <TextBox x:Name="CodeTextBox" Background="Transparent" BorderThickness="0" FontSize="14" Foreground="{DynamicResource TextBrush}"
                         Padding="4" VerticalContentAlignment="Center" KeyDown="CodeTextBox_KeyDown" />
            </Border>

            <TextBlock x:Name="ErrorText" Grid.Row="1" Margin="4,52,0,0" Foreground="#E53E3E" FontSize="12" Visibility="Collapsed"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                <Button Content="Hủy" Width="90" Height="34" Margin="0,0,10,0" Background="#E2E8F0" Foreground="#1F2937" BorderThickness="0" Click="OnCancelClick"/>
                <Button Content="Tham gia" Width="110" Height="34" Background="#4299E1" Foreground="White" BorderThickness="0" Click="OnJoinClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
