<Window x:Class="MindmapApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MindmapApp - Đăng nhập"
        WindowStartupLocation="CenterScreen"
        Height="560"
        Width="920"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">
    <Grid>
        <Grid.Effect>
            <DropShadowEffect BlurRadius="40" Opacity="0.25" ShadowDepth="0" />
        </Grid.Effect>
        <Border Background="{StaticResource BackgroundBrush}" CornerRadius="26" Padding="32">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2.2*" />
                </Grid.ColumnDefinitions>
                <Border Background="{StaticResource PrimaryBrush}" CornerRadius="20" Margin="0,0,28,0" Padding="36">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" Opacity="0.15" ShadowDepth="0" />
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal"  VerticalAlignment="Top">
                            <Ellipse Width="12" Height="12" Fill="#FF9AD1FF" Margin="0,0,10,0"/>
                            <Ellipse Width="12" Height="12" Fill="#FFFCE38A" Margin="0,0,10,0"/>
                            <Ellipse Width="12" Height="12" Fill="#FF89F7FE" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" VerticalAlignment="Center" >
                            <TextBlock Text="MindmapApp" Foreground="White" FontSize="42" FontWeight="Bold" Margin="0,0,0,6" />
                            <TextBlock Text="Biến mọi ý tưởng thành sơ đồ tư duy đầy màu sắc" Foreground="#CCFFFFFF" FontSize="18" TextWrapping="Wrap" Margin="0,0,0,18"/>
                            <StackPanel Orientation="Horizontal" >
                                <Border Background="#1AFFFFFF" Padding="12,10" CornerRadius="16" Margin="0,0,12,0">
                                    <StackPanel>
                                        <TextBlock Text="Tùy biến giao diện" Foreground="White" FontWeight="SemiBold" />
                                        <TextBlock Text="Hơn 20 màu sắc và hình dạng" Foreground="#DDFFFFFF"  FontSize="12"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#1AFFFFFF" Padding="12,10" CornerRadius="16">
                                    <StackPanel>
                                        <TextBlock Text="Kết nối trực quan" Foreground="White" FontWeight="SemiBold" />
                                        <TextBlock Text="Kéo thả siêu mượt" Foreground="#DDFFFFFF"  FontSize="12"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" CornerRadius="24" Padding="40,36" BorderBrush="#EEE" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Chào mừng trở lại 👋" FontSize="26" FontWeight="Bold" Foreground="#333" />
                        <TextBlock Text="Đăng nhập để quản lý mindmap của bạn" Margin="0,6,0,24" Foreground="#7A8699" />
                        <StackPanel>
                            <TextBlock Text="Email" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="38" Padding="8" BorderBrush="#D0D5DD" BorderThickness="1" Background="#FAFBFC"/>
                        </StackPanel>
                        <StackPanel Margin="0,14,0,0">
                            <TextBlock Text="Mật khẩu" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,4"/>
                            <PasswordBox x:Name="PasswordBox" Height="38" Margin="0,8,0,0" Padding="8" Background="#FAFBFC" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" Margin="0,10,0,0" TextWrapping="Wrap" FontSize="13"/>
                        <Button x:Name="LoginButton" Content="Đăng nhập" Margin="0,20,0,0" Click="LoginButton_OnClick" Height="36" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                            <TextBlock Text="Quên mật khẩu?" Foreground="#FF6F7D93" Margin="0,0,6,0" />
                            <Button Content="Gửi OTP" Background="Transparent" Foreground="{StaticResource PrimaryBrush}" BorderThickness="0" Click="ForgotButton_OnClick"  FontWeight="SemiBold" Padding="4,0"/>
                        </StackPanel>
                        <Border Background="#FFF1F5FB" CornerRadius="16" Padding="16" Margin="0,10,0,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                                <TextBlock Text="Chưa có tài khoản?" Margin="0,0,4,0" Foreground="#444"/>
                                <Button Content="Đăng ký ngay" Background="Transparent" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" BorderThickness="0" Click="RegisterButton_OnClick" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>