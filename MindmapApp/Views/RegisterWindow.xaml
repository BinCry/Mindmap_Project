<Window x:Class="MindmapApp.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng ký MindmapApp"
        WindowStartupLocation="CenterOwner"
        Width="520"
        Height="640"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">
    <Grid>
        <Grid.Effect>
            <DropShadowEffect BlurRadius="30" Opacity="0.2" ShadowDepth="0" />
        </Grid.Effect>
        <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="24" Padding="32">
            <StackPanel>
                <TextBlock Text="Tạo tài khoản mới" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text="Khám phá kho thư viện mindmap thông minh" Foreground="{DynamicResource TextBrush}" Opacity="0.7" Margin="0,6,0,20" />

                <StackPanel Margin="0,12,0,0">
                    <TextBlock Text="Email" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,4,0,4"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="8" BorderThickness="1"
                             Background="{DynamicResource BackgroundBrush}"
                             Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource TextBrush}"
                             CaretBrush="{DynamicResource TextBrush}"/>
                </StackPanel>

                <StackPanel Margin="0,12,0,0">
                    <TextBlock Text="Tên hiển thị" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,4,0,4"/>
                    <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="8" BorderThickness="1"
                             Background="{DynamicResource BackgroundBrush}"
                             Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource TextBrush}"
                             CaretBrush="{DynamicResource TextBrush}"/>
                </StackPanel>

                <StackPanel Margin="0,12,0,0">
                    <TextBlock Text="Mật khẩu" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,4,0,4"/>
                    <Grid Margin="0,4,0,0">
                        <PasswordBox x:Name="PasswordBox" 
                                     Style="{StaticResource ModernPasswordBox}"
                                     Padding="8,8,35,8" BorderThickness="1"
                                     Background="{DynamicResource BackgroundBrush}"
                                     Foreground="{DynamicResource TextBrush}"
                                     BorderBrush="{DynamicResource TextBrush}"
                                     CaretBrush="{DynamicResource TextBrush}"/>

                        <TextBox x:Name="VisiblePasswordBox" Visibility="Collapsed"
                                 Style="{StaticResource ModernTextBoxReveal}"
                                 Padding="8,8,35,8" BorderThickness="1"
                                 Background="{DynamicResource BackgroundBrush}"
                                 Foreground="{DynamicResource TextBrush}"
                                 BorderBrush="{DynamicResource TextBrush}"
                                 CaretBrush="{DynamicResource TextBrush}"/>

                        <ToggleButton Style="{StaticResource EyeToggleButtonStyle}"
                                      HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0"
                                      Checked="ShowPassword_Checked" Unchecked="ShowPassword_Unchecked"/>
                    </Grid>
                </StackPanel>

                <StackPanel Margin="0,12,0,0">
                    <TextBlock Text="Nhập lại mật khẩu" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,4,0,4"/>
                    <Grid Margin="0,4,0,0">
                        <PasswordBox x:Name="ConfirmPasswordBox" 
                                     Style="{StaticResource ModernPasswordBox}"
                                     Padding="8,8,35,8" BorderThickness="1"
                                     Background="{DynamicResource BackgroundBrush}"
                                     Foreground="{DynamicResource TextBrush}"
                                     BorderBrush="{DynamicResource TextBrush}"
                                     CaretBrush="{DynamicResource TextBrush}"/>

                        <TextBox x:Name="VisibleConfirmPasswordBox" Visibility="Collapsed"
                                 Style="{StaticResource ModernTextBoxReveal}"
                                 Padding="8,8,35,8" BorderThickness="1"
                                 Background="{DynamicResource BackgroundBrush}"
                                 Foreground="{DynamicResource TextBrush}"
                                 BorderBrush="{DynamicResource TextBrush}"
                                 CaretBrush="{DynamicResource TextBrush}"/>

                        <ToggleButton Style="{StaticResource EyeToggleButtonStyle}"
                                      HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0"
                                      Checked="ShowConfirmPassword_Checked" Unchecked="ShowConfirmPassword_Unchecked"/>
                    </Grid>
                </StackPanel>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFEF5350" Margin="0,16,0,0" TextWrapping="Wrap" />
                <TextBlock Text="{Binding SuccessMessage}" Foreground="{DynamicResource PrimaryBrush}" Margin="0,4,0,0" TextWrapping="Wrap" />

                <Button Content="Đăng ký" Margin="0,10,0,0" Click="RegisterButton_OnClick" Height="38" 
                        Background="{DynamicResource PrimaryBrush}" Foreground="White" BorderThickness="0"/>

                <Button Content="Quay lại đăng nhập" Margin="0,12,0,0" Background="Transparent" 
                        Foreground="{DynamicResource PrimaryBrush}" BorderThickness="0" Click="BackButton_OnClick" Height="38" />
            </StackPanel>
        </Border>
    </Grid>
</Window>