<UserControl x:Class="MindMapApp.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MindMapApp.Views"
             xmlns:converters="clr-namespace:MindmapApp.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:ConnectionToGeometryConverter x:Key="ConnectionToGeometryConverter"/>
        <converters:ConnectionToArrowGeometryConverter x:Key="ConnectionToArrowGeometryConverter"/>
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
    </UserControl.Resources>

    <Grid>
        <Path StrokeThickness="{Binding Thickness}"
              StrokeDashArray="{Binding DashArray}"
              IsHitTestVisible="False">
            <Path.Stroke>
                <SolidColorBrush Color="{Binding StrokeColor}"/>
            </Path.Stroke>
            <Path.Data>
                <MultiBinding Converter="{StaticResource ConnectionToGeometryConverter}">
                    <Binding Path="Source.X" />
                    <Binding Path="Source.Y" />
                    <Binding Path="Source.Width" />
                    <Binding Path="Source.Height" />

                    <Binding Path="Target.X" />
                    <Binding Path="Target.Y" />
                    <Binding Path="Target.Width" />
                    <Binding Path="Target.Height" />
                </MultiBinding>
            </Path.Data>
        </Path>

        <Path IsHitTestVisible="False">
            <Path.Fill>
                <SolidColorBrush Color="{Binding StrokeColor}"/>
            </Path.Fill>
            <Path.Data>
                <MultiBinding Converter="{StaticResource ConnectionToArrowGeometryConverter}">
                    <Binding Path="Source.X" />
                    <Binding Path="Source.Y" />
                    <Binding Path="Source.Width" />
                    <Binding Path="Source.Height" />

                    <Binding Path="Target.X" />
                    <Binding Path="Target.Y" />
                    <Binding Path="Target.Width" />
                    <Binding Path="Target.Height" />

                    <Binding Path="ArrowStyle" />
                </MultiBinding>
            </Path.Data>
        </Path>
    </Grid>
</UserControl>