<Window x:Class="MindmapApp.Views.RecentMapsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MindmapApp.ViewModels"
        xmlns:models="clr-namespace:MindmapApp.Models"
        xmlns:converters="clr-namespace:MindmapApp.Converters"
        mc:Ignorable="d"
        Title="Recent Mindmaps" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#121212" Foreground="White">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- List -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Recents" FontSize="28" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            <Button HorizontalAlignment="Right" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Command="{Binding LogoutCommand}" ToolTip="Logout">
                <TextBlock Text="Logout" Foreground="#AAAAAA" TextDecorations="Underline" Cursor="Hand"/>
            </Button>
        </Grid>

        <!-- Loading Indicator -->
        <Grid Grid.Row="1" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" Panel.ZIndex="10">
            <TextBlock Text="Loading..." Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <!-- Map List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" PanningMode="VerticalOnly">
            <ItemsControl ItemsSource="{Binding RecentMaps}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" ItemWidth="220" ItemHeight="180"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:MindmapDocument}">
                        <Button BorderThickness="0" Background="Transparent" Margin="10" Command="{Binding DataContext.OpenMapCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="CardBorder" Background="#1E1E1E" CornerRadius="8" Padding="0">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Content Area -->
                                            <Grid Grid.Row="0">
                                                <!-- New Map Template -->
                                                <Grid x:Name="NewMapContent" Visibility="Collapsed">
                                                    <TextBlock Text="+" FontSize="60" Foreground="#AAAAAA" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Light"/>
                                                </Grid>
                                                <!-- Existing Map Template -->
                                                <Grid x:Name="MapContent">
                                                    <!-- Placeholder Preview -->
                                                    <Border Background="{Binding CanvasBackgroundColor, Converter={StaticResource ColorToBrushConverter}, FallbackValue='#2D2D2D'}" CornerRadius="8,8,0,0" Margin="2"/>
                                                    <!-- Simple shape representation for preview if possible, but for now just color blocks -->
                                                    <Viewbox Stretch="Uniform" Margin="20">
                                                        <Canvas Width="100" Height="60">
                                                            <Rectangle Canvas.Left="40" Canvas.Top="25" Width="20" Height="10" Fill="#555" RadiusX="2" RadiusY="2"/>
                                                            <Line X1="50" Y1="30" X2="20" Y2="10" Stroke="#444" StrokeThickness="1"/>
                                                            <Line X1="50" Y1="30" X2="80" Y2="50" Stroke="#444" StrokeThickness="1"/>
                                                        </Canvas>
                                                    </Viewbox>
                                                </Grid>
                                            </Grid>

                                            <!-- Title Area -->
                                            <StackPanel Grid.Row="1" Margin="10">
                                                <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock x:Name="DateText" Text="{Binding UpdatedAt, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" Foreground="#888888" FontSize="11" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <!-- Trigger for New Map -->
                                        <DataTrigger Binding="{Binding Id}" Value="{x:Static system:Guid.Empty}" xmlns:system="clr-namespace:System;assembly=mscorlib">
                                            <Setter TargetName="NewMapContent" Property="Visibility" Value="Visible"/>
                                            <Setter TargetName="MapContent" Property="Visibility" Value="Collapsed"/>
                                            <Setter TargetName="DateText" Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="CardBorder" Property="Background" Value="#2A2A2A"/>
                                            <Setter Property="Cursor" Value="Hand"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
